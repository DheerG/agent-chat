---
phase: 12-setup-script-updates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/setup.sh
  - README.md
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "setup.sh summary output mentions team inbox watching is enabled by default"
    - "setup.sh summary mentions TEAMS_DIR env var for overriding the teams directory"
    - "README.md Environment Variables table includes TEAMS_DIR"
    - "README.md documents team inbox watching in the Architecture section"
    - "README.md Roadmap section includes Phases 9-12"
    - "README.md status line reflects current project state"
    - "README.md test count reflects current test count"
    - "All existing setup/teardown integration tests still pass"
    - "merge-settings.cjs self-tests still pass"
  artifacts:
    - path: "scripts/setup.sh"
      provides: "Updated setup summary with team watching info"
      contains: "team"
    - path: "README.md"
      provides: "Updated documentation with team inbox watching"
      contains: "TEAMS_DIR"
  key_links:
    - from: "scripts/setup.sh"
      to: "packages/server/src/index.ts"
      via: "documents TEAMS_DIR env var that server reads"
      pattern: "TEAMS_DIR"
---

<objective>
Update setup.sh summary output and README.md to document the team inbox watcher feature from Phase 11.

Purpose: Ensure users know about team inbox watching when they run setup, and that all documentation is current.
Output: Updated setup.sh with team watching info in summary, updated README.md with TEAMS_DIR env var, architecture notes, and current roadmap/test counts.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-setup-script-updates-auto-configure-team-inbox-watcher-and-update-teardown-to-remove-it/12-CONTEXT.md

Relevant source files:
@scripts/setup.sh
@scripts/teardown.sh
@scripts/test-setup.sh
@scripts/lib/merge-settings.cjs
@README.md
@packages/server/src/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update setup.sh summary to mention team inbox watching</name>
  <files>scripts/setup.sh</files>
  <action>
Modify the summary heredoc in `scripts/setup.sh` (the `cat <<SUMMARY` block at the end) to add information about team inbox watching.

After the existing "Settings written to" section, add a new line about team watching:

```
  - Team Watching: Agent team conversations from ~/.claude/teams/ are automatically visible in the web UI
```

Also add a note about the TEAMS_DIR env var in the "Next steps" or after the summary. Something like:

```
Team conversations:
  Agent team messages from ~/.claude/teams/ are automatically
  ingested into AgentChat when the server is running.
  Override the teams directory: TEAMS_DIR=/path/to/teams
```

Keep the existing output format and style. The addition should be minimal and informative.
  </action>
  <verify>bash scripts/setup.sh /tmp/agent-chat-test-verify-$$ 2>&1; rm -rf /tmp/agent-chat-test-verify-$$</verify>
  <done>setup.sh summary mentions team inbox watching and TEAMS_DIR env var</done>
</task>

<task type="auto">
  <name>Task 2: Update README.md documentation</name>
  <files>README.md</files>
  <action>
Update `README.md` with the following changes:

1. **Status line** (line 3 area): Update to reflect all phases through 12:
   ```
   > **Status:** All phases complete — v1.0 release. Includes real-time messaging, documents, archive/restore, setup scripts, and team inbox watching.
   ```

2. **Environment Variables table**: Add `TEAMS_DIR` row:
   ```
   | `TEAMS_DIR` | `~/.claude/teams/` | Directory to watch for agent team conversations |
   ```

3. **Architecture section**: Add a bullet about team inbox watching:
   ```
   - **Team inbox watching:** The server monitors `~/.claude/teams/` for agent team messages and ingests them into channels in real-time, giving humans visibility into multi-agent coordination.
   ```

4. **Roadmap section**: Add the missing phases 9-12:
   ```
   - [x] **Phase 9:** UI Polish — CSS design tokens, WCAG AA contrast, accessibility fixes
   - [x] **Phase 10:** Fix Dogfood Bugs — Archived channel write guards, tenant upsert, test fixes
   - [x] **Phase 11:** Team Inbox Ingestion — File watcher syncing ~/.claude/teams/ messages into channels
   - [x] **Phase 12:** Setup Script Updates — Documentation and setup output for team inbox watching
   ```

5. **Test count**: Update the test count in the Testing section to reflect the actual current count. Run `pnpm test` to get the real number first, then update.

6. **Port number**: The README says `http://localhost:3000` in the Quick Start and Environment Variables sections but the actual default port is 5555. Fix to match the actual default from server code (`process.env['PORT'] ?? 5555`).
  </action>
  <verify>grep -c "TEAMS_DIR" README.md && grep -c "Phase 11" README.md && grep -c "Phase 12" README.md</verify>
  <done>README.md has TEAMS_DIR in env vars table, team watching in architecture, Phases 9-12 in roadmap, correct port number</done>
</task>

<task type="auto">
  <name>Task 3: Run existing tests to verify no regressions</name>
  <files></files>
  <action>
Run the existing integration tests to verify nothing is broken:

1. Run merge-settings self-tests:
   ```
   node scripts/lib/merge-settings.cjs --test
   ```

2. Run setup integration tests:
   ```
   bash scripts/test-setup.sh
   ```

3. Run the full server test suite:
   ```
   cd packages/server && npx vitest run
   ```

All tests must pass with zero regressions. The setup/teardown tests should still pass since we only changed the summary output (not the merge logic).
  </action>
  <verify>node scripts/lib/merge-settings.cjs --test && bash scripts/test-setup.sh</verify>
  <done>All integration tests pass, no regressions</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] setup.sh summary mentions team inbox watching
- [ ] setup.sh summary mentions TEAMS_DIR env var
- [ ] README.md Environment Variables table includes TEAMS_DIR
- [ ] README.md Architecture section mentions team inbox watching
- [ ] README.md Roadmap includes Phases 9-12
- [ ] README.md status line is current
- [ ] README.md port numbers are correct (5555, not 3000)
- [ ] merge-settings.cjs self-tests pass (8/8)
- [ ] test-setup.sh integration tests pass (6/6)
- [ ] Server test suite passes with no regressions
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Documentation accurately reflects the current state of the project
- No regressions in any test suite
</success_criteria>

<output>
After completion, create `.planning/phases/12-setup-script-updates-auto-configure-team-inbox-watcher-and-update-teardown-to-remove-it/12-01-SUMMARY.md`
</output>
