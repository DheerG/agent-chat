{
  "name": "agent-chat",
  "version": "0.1.0",
  "license": "MIT",
  "description": "Real-time web UI for watching Claude Code agent teams and swarms communicate",
  "repository": {
    "type": "git",
    "url": "https://github.com/DheerG/agent-chat.git"
  },
  "scripts": {
    "dev": "concurrently -n rust,client -c blue,green \"cargo run -p agent-chat\" \"pnpm --filter @agent-chat/client dev\"",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
